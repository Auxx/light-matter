# internal-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build internal-api` to build the library.

## Running unit tests

Run `nx test internal-api` to execute the unit tests via [Jest](https://jestjs.io).
